
<div class="page-header">
	
    <h3>
      <a ng-show="post.link" href="{{post.link}}">
        {{post.title}}
      </a>
      <span ng-hide="post.link">
        {{post.title}}
      </span>
    </h3>
    
</div>

 <span style="color:green;font-style:italic;" ng-show="post.comments.length < 1">This post has no comments yet. Soyez le premier!</span>
 
  <div ng-repeat="comment in post.comments | orderBy:'-upvotes'">
       
    <span style="color:blue">
      {{comment.author}} 
    </span>
	said...
    <span style="font-size:20px; margin-left:10px;">
      {{comment.body}}
    </span>
     <span style="color:green;cursor:pointer;font-size:18px;margin-left:10px;" class="glyphicon glyphicon-thumbs-up"
      ng-click="addVote(comment)"></span>
    {{comment.upvotes}} 
  <span am-time-ago="comment.date"></span>
  </div>

<form ng-submit="addComment()"
	ng-show="isLoggedIn()"
    style="margin-top:30px;">
    <h3>Add a new comment</h3>

    <div class="form-group">
      <input type="text"
      class="form-control"
      placeholder="Comment"
      ng-model="body"></input>
    </div>
    <button type="submit" class="btn btn-primary">Post</button>
  </form>

</br>
</br>
	
<a class="btn btn-primary" style="margin-top:30px;"href="#/home">Back to Posts</a>
	
 
